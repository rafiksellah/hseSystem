<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>État des Extincteurs</title>
    <style>
        body {
            font-family: 'DejaVu Sans', Arial, sans-serif;
            font-size: 9px;
            margin: 10px;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
            border-bottom: 2px solid #000;
            padding-bottom: 10px;
        }
        .header h1 {
            margin: 0;
            font-size: 18px;
        }
        .header .info {
            font-size: 10px;
            color: #666;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 8px;
        }
        th, td {
            border: 1px solid #000;
            padding: 3px;
            text-align: left;
        }
        th {
            background-color: #4472C4;
            color: white;
            font-weight: bold;
            text-align: center;
        }
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        .conforme {
            background-color: #c6efce;
            color: #006100;
            font-weight: bold;
        }
        .non-conforme {
            background-color: #ffc7ce;
            color: #9c0006;
            font-weight: bold;
        }
        .footer {
            position: fixed;
            bottom: 10px;
            left: 0;
            right: 0;
            text-align: center;
            font-size: 8px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>ÉTAT DES EXTINCTEURS</h1>
        <div class="info">
            Export généré le {{ date_export|date('d/m/Y à H:i') }} | 
            {{ user.zone is defined ? 'Zone: ' ~ user.zone : 'Toutes zones' }} | 
            Total: {{ extincteurs|length }} extincteur(s)
        </div>
    </div>

    <table>
        <thead>
            <tr>
                <th>N°</th>
                <th>Zone</th>
                <th>Emplacement</th>
                <th>Agent</th>
                <th>Type</th>
                <th>Capacité</th>
                <th>Date Fab.</th>
                <th>Date Épr.</th>
                <th>N° Série</th>
                <th>Validé</th>
                <th>Statut Conformité</th>
                <th>Dernière Insp.</th>
            </tr>
        </thead>
        <tbody>
            {% for extincteur in extincteurs %}
            <tr>
                <td style="text-align: center; font-weight: bold;">{{ extincteur.numerotation }}</td>
                <td style="text-align: center;">{{ extincteur.zone }}</td>
                <td>{{ extincteur.emplacement ?? '-' }}</td>
                <td>{{ extincteur.agentExtincteur ?? '-' }}</td>
                <td>{{ extincteur.type ?? '-' }}</td>
                <td style="text-align: center;">{{ extincteur.capacite ?? '-' }}</td>
                <td style="text-align: center;">{{ extincteur.dateFabrication ? extincteur.dateFabrication|date('d/m/Y') : '-' }}</td>
                <td style="text-align: center;">{{ extincteur.dateEpreuve ? extincteur.dateEpreuve|date('d/m/Y') : '-' }}</td>
                <td>{{ extincteur.numeroSerie ?? '-' }}</td>
                <td style="text-align: center;">{{ extincteur.valide ? 'OUI' : 'NON' }}</td>
                <td style="text-align: center;" class="{{ extincteur.statutConformite == 'Conforme' ? 'conforme' : (extincteur.statutConformite == 'Non conforme' ? 'non-conforme' : '') }}">
                    {{ extincteur.statutConformite }}
                </td>
                <td style="text-align: center;">
                    {% set derniere = extincteur.derniereInspection %}
                    {{ derniere ? derniere.dateInspection|date('d/m/Y') : '-' }}
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="footer">
        Système HSE - État des Extincteurs
    </div>
</body>
</html>

