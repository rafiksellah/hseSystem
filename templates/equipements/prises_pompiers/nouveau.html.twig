{% extends 'base.html.twig' %}

{% block title %}Ajouter une Prise Pompier{% endblock %}

{% block page_title %}
<i class="fas fa-fire-hydrant text-danger"></i> Ajouter une Prise Pompier
{% endblock %}

{% block body %}
{% include '_partials/datalists.html.twig' %}
<div class="container-fluid">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center">
          <h5 class="mb-0"><i class="fas fa-plus"></i> Nouvelle Prise Pompier</h5>
          <a href="{{ path('app_equipements_prises_pompiers') }}" class="btn btn-outline-secondary btn-sm">
            <i class="fas fa-arrow-left"></i> Retour
          </a>
        </div>
        <div class="card-body">
          <form method="POST">
            <div class="row">
              <div class="col-md-6 mb-3">
                <label for="zone" class="form-label">Zone <span class="text-danger">*</span></label>
                <select class="form-select" id="zone" name="zone" required>
                  <option value="">Sélectionner une zone</option>
                  {% for key, zone_name in zones_disponibles %}
                    <option value="{{ zone_name }}">{{ zone_name }}</option>
                  {% endfor %}
                </select>
              </div>

              <div class="col-md-6 mb-3">
                <label for="emplacement" class="form-label">Emplacement <span class="text-danger">*</span></label>
                <input type="text" class="form-control" id="emplacement" name="emplacement" 
                       list="emplacement-prises-options" placeholder="Saisir ou sélectionner un emplacement" required>
                <small class="text-muted">Type 2 : Saisie + Liste déroulante</small>
              </div>

              <div class="col-md-6 mb-3">
                <label for="dimatere" class="form-label">Diamètre <span class="text-danger">*</span></label>
                <input type="text" name="dimatere" id="dimatere" class="form-control" 
                       value="{{ prise.dimatere }}" placeholder="Ex: 2*45 MM, 70 MM" required>
                <small class="text-muted">Saisie libre</small>
              </div>
            </div>

            <div class="alert alert-warning">
              <i class="fas fa-info-circle"></i> Les prises pompiers n'ont pas de numérotation. L'identifiant unique est : Zone + Emplacement.
            </div>

            <div class="alert alert-info">
              <i class="fas fa-info-circle"></i> Les prises pompiers sont des points de raccordement pour les services de secours.
            </div>

            <div class="d-flex justify-content-between mt-4">
              <a href="{{ path('app_equipements_prises_pompiers') }}" class="btn btn-secondary">
                <i class="fas fa-times"></i> Annuler
              </a>
              <button type="submit" class="btn btn-primary">
                <i class="fas fa-save"></i> Enregistrer
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>
{% endblock %}

