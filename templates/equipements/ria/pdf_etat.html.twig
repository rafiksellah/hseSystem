<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>État des RIA</title>
    <style>
        body {
            font-family: 'DejaVu Sans', Arial, sans-serif;
            font-size: 10px;
            margin: 10px;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
            border-bottom: 2px solid #000;
            padding-bottom: 10px;
        }
        .header h1 {
            margin: 0;
            font-size: 18px;
        }
        .header .info {
            font-size: 10px;
            color: #666;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 9px;
        }
        th, td {
            border: 1px solid #000;
            padding: 4px;
            text-align: left;
        }
        th {
            background-color: #17a2b8;
            color: white;
            font-weight: bold;
            text-align: center;
        }
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        .footer {
            position: fixed;
            bottom: 10px;
            left: 0;
            right: 0;
            text-align: center;
            font-size: 8px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>ÉTAT DES RIA (ROBINETS D'INCENDIE ARMÉS)</h1>
        <div class="info">
            Export généré le {{ date_export|date('d/m/Y à H:i') }} | 
            Total: {{ rias|length }} RIA
        </div>
    </div>

    <table>
        <thead>
            <tr>
                <th>Numérotation</th>
                <th>Zone</th>
                <th>Agent Extincteur</th>
                <th>Diamètre (mm)</th>
                <th>Longueur (m)</th>
                <th>Validé</th>
                <th>Date Validation</th>
                <th>Validé Par</th>
            </tr>
        </thead>
        <tbody>
            {% for ria in rias %}
            <tr>
                <td style="text-align: center; font-weight: bold;">{{ ria.numerotation }}</td>
                <td style="text-align: center;">{{ ria.zone }}</td>
                <td style="text-align: center;">{{ ria.agentExtincteur ?? '-' }}</td>
                <td style="text-align: center;">{{ ria.dimatere ?? '-' }}</td>
                <td style="text-align: center;">{{ ria.longueur ?? '-' }}</td>
                <td style="text-align: center;">{{ ria.valide ? 'OUI' : 'NON' }}</td>
                <td style="text-align: center;">
                    {{ ria.dateValidation ? ria.dateValidation|date('d/m/Y H:i') : '-' }}
                </td>
                <td>
                    {{ ria.validePar ? ria.validePar.nom ~ ' ' ~ ria.validePar.prenom : '-' }}
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="footer">
        Système HSE - État des RIA
    </div>
</body>
</html>

