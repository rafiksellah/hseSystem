{% extends 'base.html.twig' %}

{% block title %}Récapitulatif Sirènes{% endblock %}

{% block page_title %}
<<<<<<< HEAD
<i class="fas fa-table text-primary"></i> Récapitulatif Sirènes
=======
<i class="fas fa-bell text-warning"></i> Tableau Récapitulatif - Sirènes
>>>>>>> 0ae0fcd2966c39ffb2310a5f9f5295022dc200be
{% endblock %}

{% block body %}
<div class="container-fluid">
<<<<<<< HEAD
    <div class="row">
        <div class="col-12">
            <div class="card shadow">
                <div class="card-header bg-warning text-dark">
                    <h5 class="mb-0">
                        <i class="fas fa-bell"></i>
                        Récapitulatif Sirènes
                    </h5>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead class="table-light">
                                <tr>
                                    <th>Numérotation</th>
                                    <th>Zone</th>
                                    <th>Emplacement</th>
                                    <th>Type</th>
                                    <th>Dernière Inspection</th>
                                    <th>Conforme / Non conforme</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for sirene in sirenes %}
                                <tr>
                                    <td><strong>{{ sirene.numerotation }}</strong></td>
                                    <td>
                                        <span class="badge {{ sirene.zone == 'SIMTIS' ? 'bg-info' : 'bg-success' }}">
                                            {{ sirene.zone }}
                                        </span>
                                    </td>
                                    <td>{{ sirene.emplacement ?? 'Non spécifié' }}</td>
                                    <td>{{ sirene.type ?? 'Non spécifié' }}</td>
                                    <td>
                                        {% set derniere = sirene.derniereInspection %}
                                        {% if derniere %}
                                        <small>
                                            {{ derniere.dateInspection|date('d/m/Y') }}<br>
                                            <span class="text-muted">par {{ derniere.inspectePar.codeAgent }}</span>
                                        </small>
                                        {% else %}
                                        <span class="text-muted">Non inspecté</span>
                                        {% endif %}
                                    </td>
                                    <td>
                                        {% set derniere = sirene.derniereInspection %}
                                        {% if derniere %}
                                            {% if derniere.valide %}
                                            <span class="badge bg-success">
                                                <i class="fas fa-check"></i> Conforme
                                            </span>
                                            {% else %}
                                            <span class="badge bg-danger">
                                                <i class="fas fa-times"></i> Non conforme
                                            </span>
                                            {% endif %}
                                        {% else %}
                                        <span class="badge bg-warning">
                                            <i class="fas fa-clock"></i> Non inspecté
                                        </span>
                                        {% endif %}
                                    </td>
                                    <td>
                                        <div class="btn-group-vertical d-flex gap-1" role="group">
                                            {% if derniere %}
                                                <a href="{{ path('app_equipements_inspection_details', {id: derniere.id}) }}" 
                                                   class="btn btn-info btn-sm" title="Voir inspection">
                                                    <i class="fas fa-eye"></i>
                                                </a>
                                                {% if is_granted('ROLE_ADMIN') %}
                                                <a href="{{ path('app_equipements_inspection_modifier', {id: derniere.id}) }}" 
                                                   class="btn btn-warning btn-sm" title="Modifier inspection">
                                                    <i class="fas fa-edit"></i>
                                                </a>
                                                {% endif %}
                                            {% else %}
                                                <a href="{{ path('app_equipements_sirenes_inspecter', {id: sirene.id}) }}" 
                                                   class="btn btn-primary btn-sm" title="Inspecter">
                                                    <i class="fas fa-clipboard-check"></i> Inspecter
                                                </a>
                                            {% endif %}
                                        </div>
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="card-footer">
                    <a href="{{ path('app_equipements_sirenes') }}" class="btn btn-secondary">
                        <i class="fas fa-arrow-left"></i> Retour à la liste
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
=======
  <div class="row">
    <div class="col-12">
      <div class="card shadow">
        <div class="card-header bg-warning text-dark d-flex justify-content-between align-items-center">
          <h5 class="mb-0">
            <i class="fas fa-table"></i> Récapitulatif Complet - Sirènes
          </h5>
          <div>
            <a href="{{ path('app_equipements_sirenes') }}" class="btn btn-dark btn-sm">
              <i class="fas fa-arrow-left"></i> Retour à la liste
            </a>
          </div>
        </div>

        <div class="card-body">
          <!-- Filtres -->
          <form method="GET" class="row g-3 mb-4">
            <div class="col-md-4">
              <label for="zone" class="form-label">Zone</label>
              <select name="zone" id="zone" class="form-select form-select-sm">
                <option value="">Toutes les zones</option>
                {% for key, zone_name in constant('App\\Entity\\Sirene::ZONES_SIRENE_SUGGESTIONS') %}
                  <option value="{{ zone_name }}" {{ search_params.zone == zone_name ? 'selected' : '' }}>{{ zone_name }}</option>
                {% endfor %}
              </select>
            </div>
            <div class="col-md-3">
              <label for="numerotation" class="form-label">Numérotation</label>
              <input type="text" name="numerotation" id="numerotation" class="form-control form-control-sm"
                value="{{ search_params.numerotation }}" placeholder="Rechercher...">
            </div>
            <div class="col-md-3">
              <label for="conformite" class="form-label">Conformité</label>
              <select name="conformite" id="conformite" class="form-select form-select-sm">
                <option value="">Tous</option>
                <option value="conforme" {{ search_params.conformite == 'conforme' ? 'selected' : '' }}>Conformes</option>
                <option value="non_conforme" {{ search_params.conformite == 'non_conforme' ? 'selected' : '' }}>Non conformes</option>
                <option value="non_inspecte" {{ search_params.conformite == 'non_inspecte' ? 'selected' : '' }}>Non inspectés</option>
              </select>
            </div>
            <div class="col-md-2 d-flex align-items-end">
              <button type="submit" class="btn btn-primary btn-sm me-2">
                <i class="fas fa-search"></i> Filtrer
              </button>
              <a href="{{ path('app_equipements_sirenes_recapitulatif') }}" class="btn btn-secondary btn-sm">
                <i class="fas fa-redo"></i>
              </a>
            </div>
          </form>

          <!-- Statistiques rapides -->
          <div class="row mb-3">
            <div class="col-md-12">
              {% set total = sirenes|length %}
              {% set conformes = sirenes|filter(s => s.statutConformite == 'Conforme')|length %}
              {% set non_conformes = sirenes|filter(s => s.statutConformite == 'Non conforme')|length %}
              {% set non_inspectes = sirenes|filter(s => s.statutConformite == 'Non inspecté')|length %}
              
              <div class="alert alert-info">
                <strong>Total: {{ total }}</strong> | 
                <span class="text-success"><i class="fas fa-check-circle"></i> Conformes: {{ conformes }}</span> | 
                <span class="text-danger"><i class="fas fa-times-circle"></i> Non conformes: {{ non_conformes }}</span> | 
                <span class="text-secondary"><i class="fas fa-question-circle"></i> Non inspectés: {{ non_inspectes }}</span>
              </div>
            </div>
          </div>

          <!-- Tableau récapitulatif -->
          {% if sirenes is empty %}
            <div class="alert alert-warning text-center">
              <i class="fas fa-info-circle"></i> Aucune sirène trouvée.
            </div>
          {% else %}
            <div class="table-responsive">
              <table class="table table-striped table-hover table-bordered">
                <thead class="table-dark">
                  <tr>
                    <th style="width: 8%">N°</th>
                    <th style="width: 12%">Numérotation</th>
                    <th style="width: 15%">Zone</th>
                    <th style="width: 15%">Emplacement</th>
                    <th style="width: 10%">Type</th>
                    <th style="width: 15%">Dernière Inspection</th>
                    <th style="width: 15%" class="text-center">
                      <i class="fas fa-clipboard-check"></i> CONFORMITÉ
                    </th>
                    <th style="width: 10%">Actions</th>
                  </tr>
                </thead>
                <tbody>
                  {% for sirene in sirenes %}
                    {% set conformite = sirene.statutConformite %}
                    <tr class="{{ conformite == 'Non conforme' ? 'table-danger' : (conformite == 'Conforme' ? 'table-success' : '') }}">
                      <td><strong>{{ loop.index }}</strong></td>
                      <td><strong>{{ sirene.numerotation }}</strong></td>
                      <td><span class="badge bg-warning text-dark">{{ sirene.zone }}</span></td>
                      <td>{{ sirene.emplacement ?? '-' }}</td>
                      <td>{{ sirene.type ?? '-' }}</td>
                      <td>
                        {% set derniere = sirene.derniereInspection %}
                        {% if derniere %}
                          {{ derniere.dateInspection|date('d/m/Y H:i') }}
                          <br><small class="text-muted">par {{ derniere.inspectePar.fullName }}</small>
                        {% else %}
                          <span class="text-muted">Jamais inspecté</span>
                        {% endif %}
                      </td>
                      <td class="text-center">
                        {% if conformite == 'Conforme' %}
                          <h5 class="mb-0">
                            <span class="badge bg-success px-3 py-2">
                              <i class="fas fa-check-circle"></i> CONFORME
                            </span>
                          </h5>
                        {% elseif conformite == 'Non conforme' %}
                          <h5 class="mb-0">
                            <span class="badge bg-danger px-3 py-2">
                              <i class="fas fa-times-circle"></i> NON CONFORME
                            </span>
                          </h5>
                        {% else %}
                          <h5 class="mb-0">
                            <span class="badge bg-secondary px-3 py-2">
                              <i class="fas fa-question-circle"></i> NON INSPECTÉ
                            </span>
                          </h5>
                        {% endif %}
                      </td>
                      <td>
                        <div class="btn-group btn-group-sm" role="group">
                          <a href="{{ path('app_equipements_sirene_details', {id: sirene.id}) }}" 
                             class="btn btn-outline-primary" title="Détails">
                            <i class="fas fa-eye"></i>
                          </a>
                          <a href="{{ path('app_equipements_sirenes_inspecter', {id: sirene.id}) }}" 
                             class="btn btn-outline-success" title="Inspecter">
                            <i class="fas fa-clipboard-check"></i>
                          </a>
                        </div>
                      </td>
                    </tr>
                  {% endfor %}
                </tbody>
              </table>
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </div>
</div>
{% endblock %}

>>>>>>> 0ae0fcd2966c39ffb2310a5f9f5295022dc200be
